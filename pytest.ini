[pytest]
# Put the project root on sys.path so tests can `from src import llm`
# regardless of how pytest is invoked (bare `pytest`, `uv run pytest`, or
# `python -m pytest`).
pythonpath = .
testpaths = tests
